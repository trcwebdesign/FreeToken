# FreeToken Runtime Notes

- Windows launcher runs from `C:\Users\USER\AppData\Local\FreeToken\venv\Lib\site-packages\freetoken`; source edits must be synced there for `ft serve`.
- Gemma-4 compressed-tensors FP8: routed experts load through the legacy `_compressed_tensors_banks()` provider into BF16 banks; dense FP8 Linear layers retain raw FP8 weights plus flattened 1D scales; Gemma shared MLP is loaded BF16; Gemma attention QKV/O projections use `config.quant`; router remains BF16 when checkpoint has no router scale.
- Native Gemma NVFP4 GGUF uses `parse_gguf_config`, `moe_weight_format="nvfp4"`, and `_Nvfp4GatedMLP`; helper exists in `models/gemma4/moe.py`.
- Qwen3.5-MoE architecture alias `Qwen3_5MoeForCausalLM` is registered to `freetoken.models.qwen3_5_moe`; stale `qwen3_5_moe/weight*.pyd` can shadow Python and must be disabled when outdated.
- Qwen3.5 NVFP4 expert detection checks both `model.layers...` and `model.language_model.layers...` roots.
- Merge conflict resolved in `models/gemma4/weight.py` and `models/qwen4_exp/weight.py`: preserve upstream vision filtering/prefix support together with local ShardReader and quantized loading. No unresolved Git conflicts remain after staging those files.
- Verified syntax with the installed Python 3.12 runtime using `python -m py_compile` on resolved loaders/registry.
- After the upstream multimodal merge, `EngineConfig.model_config` must not use `dataclasses.replace()` on frozen Transformer/GGUF config shims: it drops dynamic `quantization_config` and causes Gemma FP8 to become `NoQuantConfig`. Use a shallow copy plus `object.__setattr__` for disabled encoder fields.
- Windows has no `os.posix_fadvise`; `models/loader.py::drop_page_cache()` must return early when unavailable.
- Qwen3.5 ModelOpt checkpoints can store tensors under `model.language_model.layers...` while quant metadata uses logical `model.layers...`; both `QuantConfig.scheme_for()` and `qwen3_5_moe/_DenseReader.stored()` need the logical-root fallback.
- ModelOpt weight-only FP8/NVFP4 exports omit activation `input_scale`; `ModelOptConfig` defaults `with_input_scale=False` and uses an `FP8_NO_INPUT` scheme. Otherwise Qwen GDN fusion expects nonexistent `input_scale` keys.
- Qwen3.5 loader fusion must treat `input_scale` as optional when absent, while still consuming it if present.
- Current runtime synchronization copies all `.py` files from `C:\code\FreeToken\python\freetoken` to the FreeToken wheel `site-packages`; compiled model `.pyd` files can shadow Python and should be checked after sync.
- On 2026-09-15, the eight Qwen/GGUF registry and parser `.py` files were copied directly to `C:\Users\USER\AppData\Local\FreeToken\venv\Lib\site-packages\freetoken`; source/runtime hashes matched and no active `.pyd` overlapped the touched modules.
- The repaired Qwen GGUF now resolves as `Qwen3MoeForCausalLM`; `cached_load_hf_config()` and `_spec_for_model_path()` both succeeded with 40 layers, hidden size 2048, 256 experts, top-k 8, and vocab 248320.
- The exact `ft serve` command reached `Application startup complete` and `Uvicorn running on http://0.0.0.0:1919`; the temporary smoke-test processes were then stopped. Prefer `--moe-strategy hybrid` because `--moe-backend` is deprecated.
- On 2026-09-15, native Qwen3.6/Qwen3.5 GGUF loading was added in `models/qwen3_5_moe/weight.py`: GGUF NVFP4 tensors are iterated directly, GDN `in_proj` and full-attention QKV projections are fused, shared experts stay packed, and routed experts load through native NVFP4 host banks.
- Qwen GGUF router tensors (`mlp.gate` and `shared_expert_gate`) are stored as NVFP4 but the model expects dense BF16, so the loader dequantizes only those routers.
- Qwen GDN state tensors (`linear_attn.conv1d.weight`, `A_log`, `dt_bias`, `norm.weight`) must be loaded as FP32; loading them as BF16 causes strict state-dict assertions.
- Qwen GGUF model conversion uses `qo_attn_dim` and `kv_attn_dim` from `Qwen3_5Attention`; the earlier Gemma-style `q_dim`/`kv_dim` names were invalid.
- Qwen3.5 full-attention QKV includes the gated query width (`2 * qo_attn_dim`); NVFP4 conversion must use `attn._qkv_split`, not `[qo_attn_dim, kv_attn_dim, kv_attn_dim]`. Otherwise the ten full-attention layers fail with a packed-weight shape assertion.
- The repaired GGUF has 40 layers, hidden size 2048, 256 experts, top-k 8, and vocab 248320. Native dense iterator and expert-bank shape checks pass, and the active runtime files were synced without reinstalling the wheel.
- The remaining end-to-end serving/inference check was not completed: the last server run progressed past model construction and then needed further validation of the full expert-bank/state-load path. The direct dense state-dict harness intentionally reports missing routed expert keys when run without offload banks.
- Qwen GGUF tokenizer metadata has `unknown_token_id=0`, while token ID 0 is `!` and `<unk>` is absent from the GPT-2/BPE vocabulary. `models/gguf/tokenizer.py` must use the existing metadata token as the BPE `unk_token`; hardcoding `<unk>` causes `Unk token '<unk>' not found in the vocabulary`.
- The Qwen embedded vocabulary is GPT-2 byte-level BPE, not SentencePiece. The tokenizer must use `ByteLevel(add_prefix_space=False)` and the `ByteLevel` decoder. Replacing spaces with `▁` caused `Hi there` to encode with token 0 (`!`) and generated garbage. Direct roundtrip tests for `Hi`, `Hi there`, `Say hello.`, and Japanese text pass after the fix; the active runtime was synced.
- The repaired GGUF omits Qwen tokenizer.json added_tokens: it has 248044 base BPE entries while the model vocab has 248320 rows. `models/gguf/tokenizer.py` restores the 33 Qwen added/special tokens at IDs 248044+, so `<|im_start|>`, `<|im_end|>`, `<|endoftext|>`, tool tokens, and vision tokens remain atomic. Direct ID checks pass.
- Qwen generation config expects EOS IDs `{248044, 248046}` (`<|endoftext|>` and `<|im_end|>`). `gguf_eos_token_ids()` now resolves `<|im_end|>` through the completed tokenizer vocabulary; omitting it let generation emit a new `<|im_start|>` turn and repeat garbage after the assistant answer.
- The GGML NVFP4 raw layout is confirmed from llama.cpp: four 16-value groups per 64-value block, each group stored as eight low nibbles followed by eight high nibbles. `models/gguf/nvfp4.py::nvfp4_parts()` reconstructs adjacent pairs for inline/MoE dequant; it also has a `dense=True` word-order path for the dense int32 kernel, which consumes each 8-code half as `[0,4,1,5,2,6,3,7]`. Qwen input embeddings must use the normal layout, while the Qwen LM head and dense projections use the dense layout. `models/gguf/dequant.py::dequant_nvfp4()` uses the normal GGML order.
- The immediately preceding bytewise-adjacent raw interpretation was wrong and caused a garbage-token regression after `Hi`; it was reverted on 2026-09-15 after checking llama.cpp's `quantize_row_nvfp4_ref()` and `dequantize_row_nvfp4()`.
- On 2026-09-15, 16 modified FreeToken Python package files, including `models/gguf/nvfp4.py` and `models/gguf/dequant.py`, were copied directly from `C:\code\FreeToken\python\freetoken` to `C:\Users\USER\AppData\Local\FreeToken\venv\Lib\site-packages\freetoken`; SHA-256 hashes matched. Do not use `pip install`, wheel installation, or pytest for this runtime sync; the workspace venv currently has no pytest module.
- The corrected NVFP4 helper and Qwen loader pass `py_compile` with the actual FreeToken runtime interpreter and were directly synced to site-packages. Repeated live `Hi` tests, including `reasoning_effort=none`, still produce garbage logits. The current server logs show `moe_strategy=hybrid` with a CPU NVFP4 executor (`fmt=nvfp4`), so the next discriminating test is `--moe-strategy fused` or `offload` to isolate the CPU/hybrid expert-bank path from the GPU dense path.
- The active GGUF has `general.architecture=qwen3`, tensor type 40 NVFP4, no `freetoken.gguf_format` marker, and its first inspected NVFP4 scale bytes are unsigned values 1..9 with no bytes >=128. This makes a signed-FP8 scale-sign bug unlikely; a mislabeled ModelOpt/HF payload versus true GGML-NVFP4 export remains a primary hypothesis.

- On 2026-09-15, the local Qwen3.6-Fallen-Fabulist-35B-A3B-heretic.i1-Q4_K_M.gguf server reached serving state on port 1919, but both chat and raw completion for `Hi` returned deterministic garbage. The raw completion bypassed chat templating/reasoning parsing, proving the corruption is in logits/model execution rather than API formatting.
- The Q4_K checkpoint layout is confirmed: 41 layers, 30 GDN linear-attention layers, 11 full-attention layers, hidden size 2048, 256 experts, top-k 8; embeddings are Q4_K and output head Q6_K; routed gate/up are Q4_K and routed down is Q6_K.
- In hybrid mode, routed experts are currently materialized and executed as BF16 (`CPU MoE executor ... fmt=bf16 H=2048 I=512`), so the added native `q4_k` MoE dispatch is unreachable for this run. Native CUDA Q4_K dequantization matches the installed `gguf` reference (about 4e-5 max absolute error), and Q4_K GEMV matches dequantize-then-matmul within about 0.55% relative L2.
- The remaining Q4_K correctness issue is therefore not basic dequantization, GEMV, startup, or chat formatting. The next debugging target is the Qwen3.5 state/architecture contract, especially GDN state/order, rotary behavior, norm weights, and a reference comparison; remember Windows `ft serve` may use the site-packages copy rather than workspace source.
- PR 131 review on 2026-09-15: its qwen35moe GGUF adapter identifies critical older-build bugs: GGUF RMSNorm tensors are already effective 1+w (do not add 1), `ssm_a` stores A=-exp(A_log) (load `log(-a)`), llama.cpp tiles GDN V-head rows/columns when 16 K heads map to 32 V heads (untile rows for qkv/gate/alpha/beta and columns for ssm_out), and ssm_out must be materialized dense when column untile crosses quant blocks. The current older loader was missing these contracts.
- Ported those PR 131 fixes into the source and synchronized Windows site-packages: removed extra norm shifts, transformed/untiled GDN state, untiled GDN qkv/gate/alpha/beta, and made ssm_out dense with column untile. Source syntax and runtime startup pass. A live raw `Hi` completion changed but remained incoherent (`Canti近期hte...iller...`), so PR 131 is necessary but not sufficient for this exact Q4_K_M model; remaining debugging should focus on expert dispatch/bank semantics or another Qwen GGUF contract.
- Final Qwen3.6 GGUF fix on 2026-09-15: the missing corruption source was the GDN `ssm_conv1d.weight` V-head channel untile. PR 131 required un-tiling the V channels for qkv/gate/alpha/beta, conv1d, and ssm_out; the older port had omitted conv1d. After adding it, raw `Hi` became coherent English instead of multilingual garbage.
- Final tokenizer fix: existing Qwen control tokens already present in GGUF were not marked as special, so `<|im_end|>` could appear visibly as `|im_end|>`. `models/gguf/tokenizer.py` now marks all reconstructed and existing Qwen control tokens as `AddedToken(..., special=True)`.
- Final live verification after syncing the Windows site-packages runtime: server reached serving on port 1919; chat request `Hi` with `reasoning_effort=none` returned coherent output (`Hello! How can I assist you today?`), with no prior garbage-token stream. Raw GGUF count remains 41, served decoder count is correctly 40 because block 40 is the NextN/MTP block.
- Qwen/FTW regression fixed on 2026-09-15: `parse_config()` had globally assigned `moe_weight_format="q4_k"` to normal HF/FTW configs, causing FTW NVFP4 models to allocate GGUF `qweight` buffers and fail on `model.embed_tokens.qweight`. The fix limits native GGUF conversion to configs with `is_gguf`, and preserves `gguf_model_path` through `parse_gguf_config` (the intermediate GGUF config had stored it as `gguf_model_path` but parse_config only read `model_path`). Verified: real Qwen GGUF => `moe_weight_format=q4_k`, path set; Ornith FTW NVFP4 => `moe_weight_format=None`, path None, `expert_quant=nvfp4`; wheel runtime hash matches source.
- On 2026-09-16, Qwen3.6 experts-only NVFP4 GGUF conversion was verified. The quantizer command must use default `--type nvfp4` with first-match rule `model\.language_model\.layers\..*\.mlp\.experts\.(gate_up_proj|down_proj)=nvfp4` and a later BF16 catch-all for `model\.language_model\..*`; Qwen experts are stacked tensors without numeric expert indices or `.weight` suffixes. Output contained 1026 tensors: 164 NVFP4 expert tensors and 862 BF16 tensors. The quantizer used CPU fallback for NVFP4 conversion on cuda0 but completed successfully.
- Added Qwen-specific metadata repair in `scripts/repair_qwen_gguf.py`. It writes `general.architecture=qwen35moe`, `qwen35moe.block_count=41`, `qwen35moe.nextn_predict_layers=1`, Qwen geometry/expert/SSM metadata, tokenizer vocabulary/merges, and chat template without changing tensor data. Repaired output: `C:\models\Qwen3.6-Fallen-Fabulist-35B-A3B-heretic-experts-nvfp4-metadata.gguf`.
- Validated repaired GGUF: architecture `qwen35moe`, raw blocks 41, served decoder layers 40, 256 experts, top-k 8, chat template present, 248044 tokenizer entries, type counts `{BF16: 862, NVFP4: 164}`. Use the repaired metadata file with FreeToken, not the raw tensor-only quantizer output.
- On 2026-09-16, experts-only NVFP4 Qwen GGUF live testing on port 1919 still returned repeated garbage (`тр тр тр...`) even after the direct NVFP4 dequant comparison showed correct expert values. The CPU hybrid executor was identified as the remaining suspect. The NVFP4 VNNI/W4A8 fast path in `kernel/csrc/cpu_moe/cpu_moe_ext.cpp` was disabled to force the W4A16 dequant path, and the native `_cpu_moe` extension was successfully rebuilt with CUDA library path fixes plus `/DNOMINMAX`; the rebuilt `.pyd` was copied into the active FreeToken site-packages runtime. A subsequent live test still showed garbage, so the CPU W4A16 path or another hybrid-specific bank/activation contract remains unresolved. The GPU-only comparison server was attempted on port 1920 but did not become ready during the test window and was terminated.
- On 2026-09-16, GPU-only and CPU-hybrid inference both produced repeated garbage for the experts-only NVFP4 Qwen GGUF, so the CPU kernel alone was not the root cause. A bitwise NVFP4 layout comparison against `gguf.quants.dequantize` matched exactly (max error 0, cosine ~1.0). The new GGUF was converted directly from HF Safetensors, so Qwen/Gemma RMSNorm tensors retain raw `w` and require `1+w`; the loader had treated them like legacy llama.cpp GGUF norms that are already shifted. The HF-named GGUF loader now adds `+1` to input/post/Q/K norms and final model norm, while legacy `blk.*` norms remain unchanged. Runtime file was synchronized and compiled.
- Final 2026-09-16 retest after the HF-order correction: GPU-only `offload` served successfully on port 1919 but `Hi` still generated repeated multilingual garbage (`很高兴...厂厂厂...`). Direct comparison of HF versus GGUF dense tensors was byte-exact for layer-0 GDN `A_log`, conv1d, qkv/z/a/b, input norm, embedding, and LM head. Converted expert stacks have the expected shapes `[256,1024,2048]` and `[256,2048,512]`; remaining issue is isolated to NVFP4 expert conversion/runtime semantics, likely expert scaling/packing or the MoE execution contract. The test server was stopped.
- Latest focused NVFP4 validation on 2026-09-16: `nvfp4_parts()` produces the expected bank shapes and the actual GGUF bank gives matching serial and Marlin decode results. Explicit FreeToken Triton dequant plus BF16 gate/up/down matmul agrees with the fused expert kernel to about 0.3% relative error. Router and shared-expert BF16 tensors, including gate/up fusion, are byte-exact against HF. Therefore no safe correction was found in FreeToken packing, bank reshaping, or Marlin dispatch; the unresolved fault is likely the quantizer/export quality or calibration contract. A final fix needs an HF reference/logit comparison or a calibrated NVFP4 re-export.
- Runtime shadowing rule on 2026-09-16: after resetting the Windows FreeToken venv, disable stale ABI-suffixed `.pyd` files by renaming them to `.pyd.disabled` rather than deleting them whenever a same-named current `.py` exists. For Gemma/Qwen serving, the confirmed conflicting files were `freetoken/models/qwen3_5_moe/weight.cp312-win_amd64.pyd`, `freetoken/models/weight.cp312-win_amd64.pyd`, `freetoken/checkpoint/ftw.cp312-win_amd64.pyd`, `freetoken/server/args.cp312-win_amd64.pyd`, `freetoken/launch.cp312-win_amd64.pyd`, `freetoken/engine/engine.cp312-win_amd64.pyd`, `freetoken/scheduler/scheduler.cp312-win_amd64.pyd`, `freetoken/moe/host_banks.cp312-win_amd64.pyd`, `freetoken/moe/cpu_executor.cp312-win_amd64.pyd`, `freetoken/moe/expert_banks.cp312-win_amd64.pyd`, `freetoken/moe/fused.cp312-win_amd64.pyd`, `freetoken/moe/offload_cache.cp312-win_amd64.pyd`, and `freetoken/server/api_server.cp312-win_amd64.pyd`. The Gemma GGUF loader also required disabling `freetoken/models/gemma4/gguf.cp312-win_amd64.pyd`; otherwise the old extension supplied an incompatible `model.embed_tokens.qweight` path. Do not disable true native kernel extensions such as `kernel/_cpu_moe.pyd`, `_pinned_tensor.pyd`, `_ple_store.pyd`, `aot.pyd`, `gguf.pyd`, `pinned.pyd`, or `utils.pyd` unless a matching Python file exists and the active import proves they shadow it. After disabling the confirmed shadows, imports resolve to current `.py` files and the Gemma server passes argument parsing and reaches model loading.
- Kernel-cache mismatch on 2026-09-16: the active runtime is FreeToken `0.1.3` with `freetoken-kernel-cache 0.1.2+cu130.g141c31a8d`. Rebuilding the cache from `freetoken-kernel-cache/` was attempted with Torch `2.11.0+cu130` and CUDA `13.0`, but the Windows NVCC build failed because the generated command combines incompatible `/std:c++17` and `-std=c++20` handling and reports `nvcc fatal: A single input file is required`. Do not rename the old cache version or edit its metadata as a fake rebuild. The supported temporary workaround is `FREETOKEN_DISABLE_KERNEL_CACHE_VERSION_CHECK=1`, persisted as a User environment variable; CUDA-major validation remains active. With that override, the Gemma command using `--moe-backend offload` reached `http://127.0.0.1:1919/health` successfully with `status=ok`, `maintenance=serving`, and runtime version `0.1.3`. A new process/daemon must inherit the User environment variable.
- Local working HF reference confirmed on 2026-09-16: `D:\models\local\llm\qwen36\Qwen3.6-Fallen-Fabulist-35B-A3B-heretic` contains the original 16 Safetensors shards, `tokenizer.json`, `config.json`, `generation_config.json`, and `chat_template.jinja`. The original tokenizer has 248044 BPE base tokens plus 33 Added-Tokens = 248077; the model embedding/LM-head size 248320 therefore includes padding, so the missing IDs `248078..248319` in the repaired GGUF tokenizer are padding and not the cause of the bad Qwen output. `Hi` tokenizes correctly as ID 12675 and round-trips.
- Qwen experts-only NVFP4 diagnosis against the local HF reference: raw completion with `temperature=0` produces wrong logits immediately (`Hi -> 资质的のを急忙急忙...`), proving the problem is before chat formatting. Active model layer ownership is correct: GDN layers `0,1,2,4,5,...,38` and full-attention layers `3,7,11,...,39`. Direct dequant comparisons at layers 0,1,10,20,30,39 show stable expert quantization error, not a single bad mapping: gate/up relative RMSE is about `11.4-13.2%`, down relative RMSE about `11.7-13.6%`. Runtime NVFP4 packing, bank reshaping, serial/Marlin decode, router/shared-expert tensors, and dense tensors were separately validated.
- Root cause found on 2026-09-16: the HF-derived Qwen GGUF final norm `model.language_model.norm.weight` is already byte-exact with the original HF effective norm, with mean about 1.625. `qwen3_5_moe/weight.py` incorrectly added `+1.0` again for `model.norm.weight`, unlike the input/post/Q/K norms. This distorted final logits while leaving input tokenization and layer mapping correct. Removed that extra shift and synchronized/compiled the fix into Windows `site-packages`.
- Final Qwen root cause found and fixed on 2026-09-16: the original HF `config.json` has `tie_word_embeddings=false`, and the GGUF contains a separate `lm_head.weight` plus `model.language_model.embed_tokens.weight`. `parse_gguf_config()` incorrectly inherited `tie_word_embeddings=True` from the GGUF config shim, causing FreeToken to tie the LM head to the input embedding and ignore the separate output head. The fix forces `tie_word_embeddings=False` whenever the GGUF has a separate `lm_head.weight`, and propagates the corrected value into the top config. After compiling/syncing `qwen3_5_moe/config.py`, a fresh server on port 1921 changed raw deterministic `Hi` output from `资质的のを急忙急忙...` to coherent `,\n\nI have a problem with`; the normal chat API with `reasoning_effort=none` returned `Hello! How can I help you today?`. This confirms the model file and NVFP4 expert conversion were not the primary fault; the critical issue was FreeToken's tied/untied LM-head mapping.